params {
    config_profile_name        = 'Test profile'
    config_profile_description = 'Minimal test dataset to check pipeline function'

    /***********************
    *    REQUIRED PARAMS   *
    ************************/

    fasta = '/mnt/d/nextflow_testing/vg_workflow/Lp_genome_Chr26-25.fasta'
    fai   = '/mnt/d/nextflow_testing/vg_workflow/Lp_genome_Chr26-25.fasta.fai'
    
    vcf   = '/mnt/d/nextflow_testing/vg_workflow/Ameral_Chr25-Chr26.vcf.gz'
    tbi   = '/mnt/d/nextflow_testing/vg_workflow/Ameral_Chr25-Chr26.vcf.gz.tbi'

    fq    = '/mnt/d/nextflow_testing/vg_workflow/*.{1,2}.fq.gz'

    /*******************************************************************************************************/

    enable_freebayes = true

    max_memory = 3.GB
    max_cpus   = 5
    max_time   = 30.min

    // saving intermediate files
    output_mode   = 'symlink'
}

docker {
    enabled         = true
    singularity.enabled = false
    userEmulation   = true
}

process {
    cpus   = { check_max( 3    * task.attempt, 'cpus'   ) }
    memory = { check_max( 3.0.GB * task.attempt, 'memory' ) }
    time   = { check_max( 4.h  * task.attempt, 'time'   ) }
}